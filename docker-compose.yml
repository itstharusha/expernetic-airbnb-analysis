version: '3.8'

services:
  dashboard:
    build: .
    container_name: airbnb_barcelona_dashboard
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: always
